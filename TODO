
build a grid
	within game-area div

pickrandomtile to light()
	check thistile.isActive
		pick again
	lightup( (duration?) )
		setClass active
		isActive= true
		activeInt = timeout
			removeClass active
			isActive = false


clickHandler
	if this.isActive
		clearInt(this.activeInt)
		this.removeClass active
		isActive = false
		this.timesClicked++

lose when all tiles are lit

function isGameLost
	var isGameLost = true;
	gametiles.forEach(tile)
		if tile.isActive = false
		isGameLost = false;
	return isGameLost


array of tiles objects with isActive, lightUp(), id, clickHandler(), activeInt timesClicked
	length = numOfGameSquares

newRound()
	timeout roundlength
		clearInt(roundInt)
		endRound()
	var roundInt = setInterval (roundlength/somenumber)s
		if isgamelost()
			endgame()
		pickrandomtiletolight()



